import 'package:flutter/material.dart';

class CommunautesPage extends StatelessWidget {
  const CommunautesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('communautes'),
      ),
      body: const Center(
        child: Text('communautes'),
      ),
    );
  }
}
